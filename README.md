# open-pr-test

Throwaway fixture repo for `open-pr`'s end-to-end run. Nothing here is real code.

## Conventions

- Money is always cents, stored as an integer. Never a float.
- Every multi-write operation runs inside a transaction.
